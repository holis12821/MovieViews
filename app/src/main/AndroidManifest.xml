<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movieviews">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieViews">
        <activity
            android:name="com.example.movieviews.presentation.ui.activity.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.example.movieviews.presentation.ui.activity.detailmovie.DetailMovieActivity"
            android:exported="false" />
        <activity
            android:name="com.example.movieviews.presentation.ui.activity.splash_screen.SplashScreenActivity"
            android:theme="@style/Theme.MovieViews.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>